// Server component — wraps the client-side dropshipping page
import DropshippingPage from "@/components/dropshipping-page";

export const metadata = { title: "Dropshipping Studio — Flowdesk" };

export default function Page() {
  return <DropshippingPage />;
}
